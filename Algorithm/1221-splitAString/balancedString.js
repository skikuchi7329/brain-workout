const balancedStringSplit = function(s) {
  const result = [];
  // const countR = 0;
  // const countL = 0;
  // const exR = /R/g;
  // const exL = /L/g;

  // const arrR = s.match(exR);
  // const arrL = s.match(exL);
  // console.log(arrL)
  for (let i = 0; i < s.length; i++)
    if(str[i] = )

}

const s = "RLRRLLRLRL";
console.log(balancedStringSplit(s));