/**
 * @param {number} n
 * @return {number[]}
 */
const sumZero = function(n) {
  const sum = 0;
  const arr = [];
  arr.length = n;
  for (let i = 0; i < arr.length; i++){
    if(n.length === n) {
      

    }
  }
  return sum;

};

const n = 5;
console.log(sumZero(n));